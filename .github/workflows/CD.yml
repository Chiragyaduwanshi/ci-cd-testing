name: CD Workflow

on:
  status:
    types:
      - success    

jobs:
  Deploy:
    runs-on: ubuntu-latest
    environment:
      name: dev

    steps:
      - name: Deploy 
        run: echo Proceeding with deployment
